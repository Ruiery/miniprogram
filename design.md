# 拼图匹配小程序 — 完整设计方案

> 日期：2026-05-24
> 开发者：睿蕊（个人全栈）
> 参考：娘娘爱拼图（古风饰品匹配类益智游戏）
> 交互方式：拖拽饰品到对应编号区域

---

## 一、产品概述

### 1.1 核心玩法
玩家在限定时间内，将底部饰品栏中的饰品**拖拽**到古风美人立绘上对应的**编号区域**。每个区域有唯一正确的饰品，全部匹配正确即通关。

### 1.2 一局游戏流程

```
选关 → 加载关卡数据 → 显示立绘+热区+饰品栏
→ 玩家拖拽匹配 → 正确：区域点亮锁定 | 错误：错误次数+1
→ 全部正确 → 胜利结算 | 时间耗尽或错误超限(5次) → 失败结算
→ 返回选关 / 重试 / 下一关
```

### 1.3 MVP 范围（第一版）

| 功能 | 包含 |
|------|------|
| 首页/选关 | ✅ |
| 游戏核心（拖拽匹配） | ✅ |
| 计时 + 错误限制 + 结算 | ✅ |
| 本地进度存储 | ✅ |
| 图鉴系统 | ❌ v2 |
| 广告/付费 | ❌ 不做 |
| 音效/BGM | ❌ v2 |
| 云端/登录 | ❌ 本地即可 |

---

## 二、技术方案

### 2.1 技术栈

| 层 | 选型 | 原因 |
|----|------|------|
| 框架 | 微信小程序原生（WXML + WXSS + JS） | 个人开发，无需跨端抽象 |
| 游戏渲染 | **CSS 绝对定位叠加**（非 Canvas） | 饰品拖拽用原生 touch 事件更自然，CSS 定位足够 |
| 数据存储 | wx.setStorageSync / getStorageSync | 无需后端，本地缓存搞定 |
| 关卡配置 | JS 模块导出 JSON | 打包在小程序包内，更新走版本迭代 |
| 构建工具 | 微信开发者工具自带 | 无需额外构建 |

### 2.2 为什么不用 Canvas？

本项目的"拼图"不是传统图片切块拼接，而是：
- 底层：一张完整的古风美人立绘（静态背景）
- 上层：若干个编号热区（透明覆盖层，标记位置和范围）
- 交互：玩家从底部饰品栏拖拽饰品到对应热区

这种场景下 **CSS 绝对定位 + touch 事件** 比 Canvas 更合适：
- 拖拽体验更流畅（原生手势支持）
- 热区用 div 即可，坐标计算简单
- 饰品用 image 组件，缩放/透明度控制方便
- 调试直观（DevTools 直接看 DOM）

### 2.3 项目目录结构

```
puzzle-miniapp/
├── app.js                    # 小程序入口
├── app.json                  # 全局配置（页面路由、窗口样式）
├── app.wxss                  # 全局样式
├── project.config.json       # 项目配置
├── sitemap.json              # 站点地图
│
├── pages/
│   ├── index/                # 首页（选关页）
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   │
│   └── game/                 # 游戏主界面
│       ├── game.wxml
│       ├── game.wxss
│       ├── game.js
│       └── game.json
│
├── components/               # 自定义组件
│   ├── timer/                # 倒计时组件
│   │   ├── timer.wxml
│   │   ├── timer.wxss
│   │   ├── timer.js
│   │   └── timer.json
│   │
│   ├── error-counter/        # 错误计数组件
│   │   ├── error-counter.wxml
│   │   ├── error-counter.wxss
│   │   ├── error-counter.js
│   │   └── error-counter.json
│   │
│   └── result-modal/         # 结算弹窗组件
│       ├── result-modal.wxml
│       ├── result-modal.wxss
│       ├── result-modal.js
│       └── result-modal.json
│
├── data/
│   ├── levels.js             # 关卡数据（所有关卡配置）
│   └── items.js              # 饰品素材库（所有饰品的元信息）
│
├── utils/
│   ├── storage.js            # 本地存储封装（进度读写）
│   ├── drag.js               # 拖拽逻辑封装
│   └── constants.js           # 常量定义
│
└── images/                   # 图片资源
    ├── bg/                   # 背景、UI 元素
    ├── characters/           # 美人立绘（按关卡命名）
    └── accessories/          # 饰品素材（按 ID 命名）
```

---

## 三、数据结构设计

### 3.1 关卡数据格式 (data/levels.js)

```javascript
// data/levels.js
module.exports = [
  {
    id: 1,                    // 关卡唯一ID
    name: "画中点翠",          // 关卡名称
    chapter: "拼图匹配",       // 所属章节
    page: 4,                  // 所在页码
    timeLimit: 355,            // 时间限制（秒），参考截图 05:55
    maxErrors: 5,              // 最大错误次数
    characterImage: "/images/characters/level_1.png",  // 立绘路径
    characterWidth: 750,        // 立绘显示宽度（rpx）
    characterHeight: 900,       // 立绘显示高度（rpx）

    // 热区定义（立绘上的可放置区域）
    zones: [
      {
        id: 1,                 // 区域编号（显示给用户看的数字）
        x: 280,                // 左上角 X 坐标（相对于立绘的百分比 %）
        y: 120,                // 左上角 Y 坐标（%）
        width: 80,             // 区域宽度（%）
        height: 60,            // 区域高度（%）
        correctItemId: "a001"  // 正确的饰品ID
      },
      {
        id: 2,
        x: 360,
        y: 120,
        width: 70,
        height: 55,
        correctItemId: "a002"
      }
      // ... 更多区域
    ],

    // 本关可用饰品池（显示在底部饰品栏）
    availableItems: ["a001", "a002", "a003", "a004", "a005",
                     "a006", "a007", "a008", "a009", "a010"]
  },
  // ... 更多关卡
]
```

### 3.2 饰品数据格式 (data/items.js)

```javascript
// data/items.js
module.exports = {
  "a001": {
    id: "a001",
    name: "翠羽步摇",          // 饰品名称
    image: "/images/accessories/a001.png",  // 饰品图片路径
    width: 100,                // 饰品原始宽度（用于缩放参考）
    height: 100                // 饰品原始高度
  },
  "a002": {
    id: "a002",
    name: "珍珠钗",
    image: "/images/accessories/a002.png",
    width: 90,
    height: 95
  }
  // ... 更多饰品
}
```

### 3.3 本地存储数据格式 (utils/storage.js)

```javascript
// 存储键名
const STORAGE_KEYS = {
  PROGRESS: 'puzzle_progress',     // 关卡进度
  SETTINGS: 'puzzle_settings'      // 用户设置（预留）
}

// 进度数据结构
// {
//   currentLevel: 3,              // 当前解锁到的关卡
//   levels: {
//     1: {                        // 关卡ID
//       unlocked: true,           // 是否已解锁
//       bestTime: 280,            // 最佳用时（秒）
//       bestErrors: 1,            // 最少错误次数
//       completed: true,          // 是否通关
//       completedAt: 1716585600   // 首次通关时间戳
//     },
//     2: {
//       unlocked: true,
//       completed: false
//     },
//     3: {
//       unlocked: false
//     }
//   }
// }
```

---

## 四、页面与组件设计

### 4.1 首页/选关页 (pages/index)

**布局：**
```
┌─────────────────────┐
│   🏮 娘娘爱拼图      │  标题/Logo
│                     │
│  ┌─────┐ ┌─────┐   │
│  │ 第1关│ │ 第2关│   │  关卡网格（3列）
│  │ ⭐   │ │ 🔒   │   │  已通关显示星
│  └─────┘ └─────┘   │  未解锁显示锁
│  ┌─────┐ ┌─────┐   │
│  │ 第3关│ │ 第4关│   │
│  │ 🔒   │ │ 🔒   │   │
│  └─────┘ └─────┘   │
│                     │
└─────────────────────┘
```

**功能：**
- 显示所有关卡卡片（网格布局）
- 已通关：显示⭐ + 最佳成绩
- 当前可玩：高亮边框
- 未解锁：灰色 + 🔒图标
- 点击已解锁关卡 → `wx.navigateTo` 进入游戏页
- 顺序解锁：完成第 N 关才解锁第 N+1 关

### 4.2 游戏主界面 (pages/game) — 核心

**完整布局：**

```
┌──────────────────────────┐
│ ← 返回   画中点翠   🔄重试│  顶部导航栏
├──────────────────────────┤
│  拼图匹配 > 第4页 > 画中点翠│  面包屑导航
├──────────────────────────┤
│                          │
│     选择正确的饰品        │  提示文字
│                          │
│  ⏱ 05:55   ❌ 0/5       │  状态栏（timer + error-counter 组件）
│                          │
│  ┌────────────────────┐  │
│  │                    │  │
│  │   古风美人立绘      │  │  character-image
│  │                    │  │
│  │  ┌──┐┌──┐         │  │
│  │  │1 ││2 │  ...    │  │  热区层（zone overlays）
│  │  └──┘└──┘         │  │  未完成显示编号
│  │                    │  │  完成显示饰品图+✓
│  │                    │  │
│  └────────────────────┘  │
│                          │
├──────────────────────────┤
│  ◀ [饰品1][饰品2][...] ▶ │  底部饰品栏（横向滚动）
│                          │
└──────────────────────────┘
```

**关键交互 — 拖拽流程：**

```
touchstart（饰品元素）
  → 记录起始位置
  → 创建拖拽副本（绝对定位，跟随手指）
  → 原饰品半透明

touchmove（拖拽中）
  → 副本跟随手指移动（setTranslateX/Y）
  → 实时检测是否悬停在某个 zone 上方
  → 目标 zone 高亮提示

touchend（释放）
  → 判断释放位置：
    ├─ 在某个 zone 内 → 触发匹配判定
    │   ├─ 正确 → 饰品吸附到 zone，锁定，播放成功动画
    │   └─ 错误 → 饰品飞回原位，错误数+1，抖动动画
    └─ 不在任何 zone 内 → 饰品飞回原位

  → 检查是否全部完成 → 触发胜利结算
  → 检查错误是否超限 → 触发失败结算
```

### 4.3 结算弹窗 (components/result-modal)

**胜利状态：**
```
┌────────────────────┐
│                    │
│     ✨ 过关啦！     │
│                    │
│   用时: 03:22      │
│   错误: 1/5        │
│                    │
│  ┌──────────┐      │
│  │  下一关   │      │  主按钮
│  └──────────┘      │
│  ┌──────────┐      │
│  │  重玩本关  │      │  次按钮
│  └──────────┘      │
│                    │
└────────────────────┘
```

**失败状态：**
```
┌────────────────────┐
│                    │
│     💪 再接再厉！   │
│                    │
│   原因: 时间耗尽    │
│        或          │
│   原因: 错误过多    │
│                    │
│  ┌──────────┐      │
│  │  重新挑战  │      │  主按钮
│  └──────────┘      │
│  ┌──────────┐      │
│  │  返回选关  │      │
│  └──────────┘      │
│                    │
└────────────────────┘
```

---

## 五、核心逻辑详解

### 5.1 拖拽引擎 (utils/drag.js)

```javascript
// 核心思路：
// 1. 使用微信小程序的 bindtouchstart/move/end 事件
// 2. 拖拽时创建一个"幽灵元素"（ghost）跟随手指
// 3. 通过 wx.createSelectorQuery() 获取各 zone 的屏幕坐标
// 4. touchend 时判断 ghost 中心点是否落在某个 zone 的 boundingClientRect 内
// 5. 命中则触发 match 逻辑，未命中则回弹

class DragEngine {
  constructor(options) {
    this.onDrop = options.onDrop        // 释放回调
    this.onHover = options.onHover      // 悬停回调（高亮目标）
    this.zones = options.zones          // 热区列表
  }

  // touchstart → 创建 ghost
  // touchmove  → 移动 ghost + 碰撞检测
  // touchend   → 判定 + 清理 ghost
}
```

### 5.2 匹配判定逻辑

```javascript
function checkMatch(zoneId, itemId) {
  const zone = levelData.zones.find(z => z.id === zoneId)
  return zone.correctItemId === itemId
}

// 正确：
// 1. 将饰品图固定到 zone 位置
// 2. zone 标记为 completed
// 3. 播放成功微动画（缩放闪烁）
// 4. 从饰品栏移除该饰品（或置灰）
// 5. 检查 allCompleted → 胜利

// 错误：
// 1. 错误计数 +1
// 2. 饰品飞回原位（动画）
// 3. zone 抖动反馈（红色闪一下）
// 4. 检查 errors >= maxErrors → 失败
```

### 5.3 计时器逻辑

```javascript
// 使用 setInterval 每秒递减
// 到 0 时触发超时失败
// 胜利时立即停止计时
// 页面 onHide/onUnload 时清除定时器（防内存泄漏）
```

### 5.4 进度存储逻辑

```javascript
// 首次进入 → 初始化默认进度（第1关解锁）
// 通关一关 → 更新该关 completed=true + 解锁下一关
// 每次打开首页 → 从 storage 读取进度刷新 UI
```

---

## 六、开发任务清单（按执行顺序）

### Phase 1：骨架 + 首页（第1天）

| # | 任务 | 产出文件 | 说明 |
|---|------|---------|------|
| 1.1 | 创建小程序项目 | 全部基础文件 | 微信开发者工具新建项目 |
| 1.2 | app.js / app.json / app.wxss | 入口文件 | 配置页面路由、全局样式 |
| 1.3 | 关卡数据 + 饰品数据 | data/levels.js, items.js | 先写 1 个测试关卡（3-5个区域） |
| 1.4 | 存储工具函数 | utils/storage.js | 进度的读写封装 |
| 1.5 | 首页/选关页 | pages/index/* | 关卡网格展示 + 解锁状态 |

### Phase 2：游戏核心（第2-3天）

| # | 任务 | 产出文件 | 说明 |
|---|------|---------|------|
| 2.1 | 游戏页面骨架 | pages/game/* | 立绘展示 + 热区覆盖层 + 饰品栏 |
| 2.2 | 拖拽引擎 | utils/drag.js | touch 事件处理 + ghost 跟随 + 碰撞检测 |
| 2.3 | 匹配判定 + 反馈 | pages/game.js | 正确/错误的逻辑 + 动画效果 |
| 2.4 | 计时器组件 | components/timer/* | 倒计时显示 + 超时处理 |
| 2.5 | 错误计数组件 | components/error-counter/* | 错误数显示 + 超限处理 |

### Phase 3：结算 + 收尾（第4天）

| # | 任务 | 产出文件 | 说明 |
|---|------|---------|------|
| 3.1 | 结算弹窗 | components/result-modal/* | 胜利/失败两种状态 |
| 3.2 | 进度保存串联 | utils/storage.js + 各页面 | 通关自动存进度 + 解锁 |
| 3.3 | 多关卡数据填充 | data/levels.js | 至少做 3-5 个完整关卡 |
| 3.4 | UI 打磨 + 动效完善 | 各页面 | 过渡动画、反馈动效、适配 |
| 3.5 | 测试 + 机型适配 | 全项目 | 不同屏幕尺寸测试 |

---

## 七、美术资源需求清单

| 资源类型 | 规格 | 数量（MVP） | 备注 |
|---------|------|-----------|------|
| 美人立绘 | PNG 透明底，750×900px 以上 | 3-5 张 | 每关一张 |
| 饰品素材 | PNG 透明底，约 100×100px | 每关 10-20 个 | 含正确项 + 干扰项 |
| 关卡卡片封面 | PNG/JPG，300×300px | 3-5 张 | 选关页用 |
| 图标 | PNG 透明底 | 若干 | 星星、锁、返回、重试等 |
| 背景 | JPG/PNG | 1-2 张 | 全局背景 |

---

## 八、关键注意事项

1. **小程序包体积限制 2MB**：图片资源要控制好，大图考虑用网络 CDN 或分包加载
2. **拖拽在真机上可能有差异**：需在真机测试 touch 事件的坐标换算
3. **热区坐标建议用百分比**：适配不同屏幕宽度（高度固定比例缩放）
4. **饰品栏横向滚动**：饰品多时需要 scroll-view
5. **page 与 component 之间通信**：使用 triggerEvent 自定义事件
